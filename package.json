{
  "name": "zzz-calculator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc -p tsconfig.app.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:data": "node scripts/validate-data.mjs",
    "minify:static": "node scripts/minify-static.mjs",
    "validate:specs": "node scripts/validate-specs.mjs",
    "specs:import": "node scripts/import-specs.mjs",
    "specs:new": "node scripts/create-agent-spec.mjs",
    "specs:bootstrap": "node scripts/bootstrap-specs.mjs",
    "specs:coverage": "node scripts/spec-coverage.mjs",
    "docs:status": "node scripts/generate-implementation-status.mjs",
    "sync:wengine-recs": "node scripts/sync-signature-wengine-recs.mjs",
    "verify:recording": "node scripts/verify-recording.mjs",
    "zc": "node scripts/zc.mjs",
    "check-guards": "node scripts/check-guards.mjs",
    "check-tokens": "node scripts/check-tokens.mjs",
    "gen:multiplier-record": "GEN_MULTIPLIER_RECORD=1 vitest run src/composables/__tests__/multiplierRecord.test.ts",
    "probe:panel": "vitest run src/core/__tests__/panelProbe.test.ts",
    "probe:calibration": "PROBE_CALIB=1 vitest run src/composables/__tests__/runArchiveCalibration.test.ts",
    "check": "npm run check-guards && npm run check-tokens && npm run validate:data && npm run validate:specs && npm test",
    "verify": "npm run check-guards && npm run check-tokens && npm run validate:data && npm run validate:specs && npm run verify:recording && npm test && npm run typecheck && npm run build"
  },
  "dependencies": {
    "@vicons/ionicons5": "^0.13.0",
    "naive-ui": "^2.41.0",
    "pinia": "^2.3.1",
    "vue": "^3.5.13",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.7.3",
    "vite": "^6.1.0",
    "vitest": "^4.1.10",
    "vue-tsc": "^2.2.0"
  }
}
