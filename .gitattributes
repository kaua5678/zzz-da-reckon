# 统一 LF：杜绝 CRLF/LF 混行导致的整文件 churn。
# 历史事故 ×2：python 文本模式 read()/write() 把 CRLF 翻成 LF，core/types 五文件整文件 diff。
* text=auto eol=lf

# 二进制资源不参与文本/行尾规范化
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
