# Dependencies and build output
node_modules/
dist/

# TypeScript / bundler artifacts
*.tsbuildinfo
scripts/check.mjs

# Local-only state
.trae-backups/
.git-invalid/
.npm-cache/
*.log

# Agent loop-mode task ledgers (working state, not project knowledge)
.claude/task-ledger*.md
.claude/ledgers/

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# ZCode session plans (working state, not project knowledge)
.zcode/

# Mimocode agent runtime (working state, not project knowledge)
.mimocode/

# workbuddy agent runtime（上一个 Windows 端 agent 的工作区；用户 2026-09-10 授权清理，留 ignore 防再冒出来）
.workbuddy/

# zc 车道租约与验证日志（工作状态，不是项目知识；check-guards 拒绝其被跟踪）
.zc/
